<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.plana">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />-->

    <!-- 【常用】 移动推送 TPNS SDK所需权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_USER_PRESENT" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--    <permission-->
    <!--        android:name="com.example.plana.permission.XGPUSH_RECEIVE"-->
    <!--        android:protectionLevel="signature" />-->
    <!--    <uses-permission android:name="com.example.plana.permission.XGPUSH_RECEIVE" />-->

    <application
        android:name=".base.MainApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlanA">

        <!-- splash -->
        <activity
            android:name=".function.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".function.MainActivity"
            android:launchMode="singleTask" />

        <!-- plan -->
        <activity android:name=".function.plan.AchievementActivity" />
        <activity android:name=".function.plan.AddPlanActivity" />
        <activity android:name=".function.plan.PlanDetailActivity" />

        <!-- schedule -->
        <activity android:name=".function.schedule.CourseActivity" />
        <activity android:name=".function.schedule.ScheduleSettingActivity" />

        <!-- setting -->
        <activity android:name=".function.setting.InfoModifyActivity" />
        <activity android:name=".function.setting.TrashBinActivity" />

        <!-- timer -->
        <activity android:name=".function.timer.TimerActivity" />
        <activity android:name=".function.timer.TimerRecorderActivity" />

        <!-- todos -->
        <activity android:name=".function.todo.AddTodoActivity" />
        <activity android:name=".function.todo.EditTodoActivity" />

        <!-- user -->
        <activity android:name=".function.user.LoginActivity" />
        <activity android:name=".function.user.RegisterActivity1" />
        <activity android:name=".function.user.RegisterActivity2" />
        <activity android:name=".function.setting.AboutActivity" />


        <!-- receiver -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!--        <service android:name="cn.leancloud.push.PushService"/>-->
        <!--        <receiver android:name="cn.leancloud.push.LCBroadcastReceiver"-->
        <!--            android:exported="true">-->
        <!--            <intent-filter>-->
        <!--                <action android:name="android.intent.action.BOOT_COMPLETED" />-->
        <!--                <action android:name="android.intent.action.USER_PRESENT" />-->
        <!--                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />-->
        <!--            </intent-filter>-->
        <!--        </receiver>-->


        <!--        <meta-data-->
        <!--            android:name="XG_SERVER_SUFFIX"-->
        <!--            android:value="tpns.sh.tencent.com" />-->

    </application>

</manifest>